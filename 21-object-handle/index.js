var student = {
    name: "Jerry", 
    age: 14,
    ole: "student"
   }

function countProperty(obj) {
     // viết code ở đây 
     console.log(Object.keys(obj).length)
   }
   countProperty(student);